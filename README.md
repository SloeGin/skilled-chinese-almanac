# 技能老黄历 (Skill Almanac)

React + Vite 构建的双语老黄历体验，结合「技能」玩法，让传统宜/忌变得更有互动感。项目支持 Firebase Hosting 部署，并提供可分享的 deterministic hash。

在线 Demo：****

## ✨ 功能概览

- 📅 月历视图：选择任意日期、快速回到今天。
- 🌐 多语言：中文 / English 即切即用，URL 也保持语言前缀。
- ✅ 宜/忌数据：结合默认词条与自定义 override，支持 locked days。
- 🪄 技能系统：抽 3 张技能卡，依据可控随机影响宜/忌，可通过 hash 分享结果。
- 🔒 技能锁定：在 override 中设置 `locked` 即可禁止当天发动技能。
- 📱 自适应：桌面与移动端都能顺畅体验。

### Override 小提示

- 可利用 override 精准插入某天的专属「宜 / 忌」，传递想说的话。
- 搭配 share hash，把特殊提醒分享给朋友，既神秘又好玩。

## 🛠️ 开发

```bash
# 安装依赖（任选其一）
pnpm install
# npm install / yarn install

# 启动本地开发
pnpm run dev
```

访问默认地址 http://localhost:5173 查看效果。

## 🧱 构建

```bash
pnpm run build
```

构建产物默认输出到 `dist/`，可直接上传到任意静态站点。

## ☁️ Firebase Hosting 部署

> 项目已包含基础 `firebase.json`。

1. 安装并登录 Firebase CLI：`npm install -g firebase-tools && firebase login`
2. 复制 `.firebaserc.example` 为 `.firebaserc` 并写入自己的项目 ID（多环境可 `firebase use --add`）
3. 一键构建并部署：
   ```bash
   pnpm run firebase:deploy
   ```

如需 rewrites / headers / SSR 等自定义规则，可继续扩展 `firebase.json`。

## 📁 主要目录结构

```
.
├── public/            # 静态资源
├── src/
│   ├── components/    # React 组件
│   ├── data/          # 宜/忌词条、override 等数据
│   ├── skills.ts      # 技能卡定义与解析
│   ├── skillHash.ts   # 分享 hash 编解码
│   └── App.tsx        # 主界面
├── dist/              # 构建输出
├── vite.config.ts
└── firebase.json
```

## 🤝 贡献

欢迎 Issue / PR，一起丰富技能或词条。提交前请：

- 运行 `pnpm run build` 确保通过
- 说明新增技能 / override 的中英文文案
- 遵守现有代码风格

## 📄 License

MIT
